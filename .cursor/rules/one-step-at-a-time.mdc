---
description: 优先把能一条命令/一条管道跑完的动作聚合成一步；只在真的需要中途看结果决定下一步时才拆
alwaysApply: true
---

# 能聚合就一步搞定；不能聚合才分步

这条规则是两次用户 feedback 的合成版。两条用户原话都记录下来防止后续把天平摆错方向。

原话 1（第一版，防止一次列一屏步骤）：
> 如果不是一步可以整体搞定的你就只提供一步的操作就可以。写很多步我不会看，下次我再问浪费 TOKEN。

原话 2（第二版，防止把能合并的也拆成一步）：
> 能一步聚合的一次搞定，不然效率极低。

**默认倾向：聚合。**  
只有当下一条命令**必须**先看见前一条的输出才能写出来（不是"想看一眼"，是参数真的只能从输出里拿到），才拆步。

## 判决树（问自己 3 个问题）

1. 下一条命令的参数能事先写死吗？
   - 可以（hardcoded 数字、shell 变量、`$SLURM_JOB_ID` 这类 salloc 子 shell 已有的环境变量）→ **聚合**
   - 不行（要 salloc 新 allocated 的 JOBID 但用户不在 salloc 的子 shell 里）→ **拆步**

2. 前一条命令失败后，下一条是否要分叉（做不同的事）？
   - 不分叉，只是后一条跑不起来 → **聚合**，用 `set -e` / `&&` 串起来，失败了一起报
   - 分叉（失败→重编 / 成功→跑 sweep）→ **拆步**

3. 用户是否明确说"我想先看这一步的输出再决定"？
   - 是 → **拆步**
   - 否 → **聚合**

三个问题全都答"聚合"的，就写成**一条 `srun bash -c '...'`** 或**一条 `scancel; salloc`** 这种形式，让用户一次粘贴完成。

## 可以聚合的典型例子

- `scancel $OLD_JOB; salloc ...` —— 两个独立操作参数已知，一条搞定，不浪费 20 秒的对话往返。
- `srun ... bash -c 'git pull; make; ls -l build/...; strings ... | grep ...'` —— 编译+验证一条链，失败链 abort。
- `srun ... -n 8 ... ep_bench ... 2>&1 | grep -E "..." | head -N` —— 跑 + 过滤 + 裁长度一条完成。
- 同一个 salloc 子 shell 里：`srun --jobid=$SLURM_JOB_ID ... make` + `srun --jobid=$SLURM_JOB_ID ... ep_bench` 可以合并进一条 `srun ... bash -c '... make ...; ... ep_bench ...'`。

## 必须拆步的典型例子

- `salloc` 的 JOBID 要等它打印出来才能在另一条独立消息里用 `--jobid=<数字>`（此时用户已经退出 salloc 子 shell，`$SLURM_JOB_ID` 不再可见）。
- Probe 有 4 种可能结果，不同结果下一步要改代码 vs 跑 sweep vs 换参数——必须看见结果再决定。
- 编译失败后要改代码，而改什么代码取决于具体 error message。

## 硬性规则

1. 默认聚合：写下一条命令前先问自己"这些动作能不能塞进一条 `bash -c '...'` / 一条管道"。能 → 合并。
2. 拆步时必须标注 `第 N 步：<动作>`，并在结尾指明**期望看到的关键输出**，保证用户贴回来的东西足够你推下一步。
3. **绝不**在拆步命令后附带"下一步预告"，用户原话明确不看。
4. 聚合命令里用 `set -e` 让任何一环失败都立刻终止整条，避免"前面失败后面还在跑浪费时间 + 刷屏"。
5. 聚合命令末尾加 `| head -N` / `grep -E` 过滤裁长度，把输出控制在可读范围内。

## 反模式（都是两次违反后的总结）

- ❌ 一次列 5 步全流程，用户不看，还下次又问。
- ❌ 明明 `scancel + salloc` 两条是独立的、没有参数依赖，还非要拆成"第 1 步 scancel、第 2 步 salloc"两条消息，把一次会话的 round-trip 数翻倍。
- ❌ 在聚合命令里把所有可能的失败分支都列出"如果 X 就做 Y"的 if/else 文字，让用户自己在头脑里 dispatch——直接跑、失败再说。
- ❌ 在拆步命令末尾加"下一步我大概会让你跑 srun xxx"的预告。
