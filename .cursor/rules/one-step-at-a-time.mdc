---
description: 需要用户在真实集群上按顺序执行多条命令时，每次只给一步；不一次列全流程
alwaysApply: true
---

# 一次只给一步

当用户要在真实环境（Slurm/GB300/jumphost/compute tray）按顺序执行多条命令、且**后一步的参数依赖前一步的实际输出**（job id / 分配到的节点名 / 编译是否成功 / probe 是否报错）时，**一条消息里只给第 1 步**。等用户把该步的输出贴回来，确认无误后，再给第 2 步。

用户的原话（记录在此防止忘掉）：
> 如果不是一步可以整体搞定的你就只提供一步的操作就可以。写很多步我不会看，下次我再问浪费 TOKEN。

## 适用场景

- 多步 Slurm 流程：`salloc` → `srun` 编译 → `srun` 跑 → 读日志
- 需要 `$SLURM_JOB_ID` 或分配到的节点名作为下一步输入
- 编译成功与否决定下一步是跑二进制还是回去改代码
- probe 的 4 种分支输出（pass / driver 不支持 / handle 不通 / setAccess 失败）决定下一步走哪条路

## 不适用场景（可以一次给完）

- 所有命令都是**不依赖前一步输出**的并列命令，且总长度不超过一屏
- 单条命令就能搞定的任务
- 纯代码/纯文档编辑，用户不需要自己执行

## 硬性规则

1. 给步骤前先判断：**下一步的命令参数是否依赖上一步的真实输出**？是 → 只给这一步。否 → 可以一次给完。
2. 给"第 N 步"时必须明确标注：`第 N 步：<动作>`，并在结尾写清楚**期望看到的关键输出**和**需要用户贴回来什么**，保证用户贴回来的东西足够你给下一步。
3. **不要**在"第 N 步"末尾附带"第 N+1 步预告/下一步大致会做什么"这类前瞻信息 —— 用户说了不看，写出来就是浪费 token。
4. 用户贴回的输出如果能直接推出下一步参数（如 job id、节点名、pass/fail 分支），**立刻给第 N+1 步**，不要重复总结前一步。
5. 如果不确定"到底是一步还是多步"，**按多步处理**，先给第 1 步问用户。

## 反模式（都是之前犯过的）

- ❌ 一口气把 `salloc + srun 编译 + srun 跑 + 读日志判断 + 如果 pass 再跑 sweep` 5 步全列出来
- ❌ 第 1 步末尾加"完成后我会给你第 2 步的命令，大致会是 xxx" —— 用户**不会**看这段
- ❌ 把 4 种可能的 probe 输出分支都罗列对应的下一步处理，让用户自己按分支走 —— 直接等用户贴输出，然后只给命中的那一支
