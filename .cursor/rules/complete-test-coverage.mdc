---
description: 跑性能测试时默认覆盖完整 (EP, tokens, modes) 网格；缩范围必须用户明确同意，且收尾要主动汇总缺什么
alwaysApply: true
---

# 测试范围必须默认完整覆盖

这条规则是 2026-04-28 当天的直接产物。原话：
> 怎么感觉测试结果少了 EP16 EP32

## 根因

我之前给的命令把 `EP_SIZES="8"` 写死成单值，理由是"快点验证 ktimer 修复"。结果用户拿到结果一看缺 EP16/EP32 数据，又得我重跑一轮。**短视的"快"反而慢**。

`run_all_from_jumphost.sh` 的 default `EP_SIZES="4 8 16 32 64"` 不是随便写的，是产线现在 sweep 的标准网格——长期 master CSV (`nccl_ep_master.csv`) 也是这套维度积累的。任何对其中一个维度的修复都应该在**完整网格**上验证一次，否则其他 size 是不是也被同样的 bug 影响了根本不知道。

## 硬性流程（产出 perf 测试命令前必做）

1. **默认值 = 仓库中验证过的全网格**：
   - `EP_SIZES`：照搬 `run_all_from_jumphost.sh` 的 default `4 8 16 32 64`（按机器排队压力可砍 64）
   - `TOKENS`：照搬 `ep_sweep.sh` 的 default `16 32 64 128 256 4096 8192`
   - `MODES`：跑对照实验时**必须包含被对照对象**（改 FM 就必须同步跑 HT 对齐基线）

2. **缩范围必须显式申请**：
   - 缩到 `EP_SIZES="8"` / 单 token 这种"快路径"前先问一句"是否只跑 EP=8 单点 quick check，跳过 16/32/64？"
   - 用户回答"是"再缩；用户没明确同意就跑全网格

3. **运行结果回传时主动汇总缺什么**：
   - 当 sweep 真的只跑了一个子集（无论用户同意或我自作主张），在结果总结里**第一行**就明确写出"本次只覆盖 EP=X tokens=Y, 网格的 {缺失维度} 没跑"
   - 让用户立刻看见数据范围而不是回头怀疑

4. **修复类 / 回归测试 → 全网格不可省**：
   - `bug fix` / `metric correction` / `algorithm change` 这三类改动 **不允许只跑单点**——bug 在不同 size/tokens 上可能表现不同
   - 例外：用户明确说"这只是 sanity check 看下能不能跑"

## 反模式（这次踩的）

- ❌ 给 `EP_SIZES="8"` 单值，验证一个 ktimer 修复，结果用户问"EP16/EP32 呢"。
- ❌ 把 `TOKENS="8192"` 单值跑，错过小 token (≤256) 时 barrier overhead 占比的关键证据。
- ❌ 跑完后总结只列报到的数据，不列**没报的维度**，让用户自己发现缺。

## 自检清单（每次给 sweep 命令必过）

- [ ] `EP_SIZES` 是否覆盖 `run_all_from_jumphost.sh` 的 default 集？（缩了就在消息里说为什么）
- [ ] `TOKENS` 是否覆盖小/中/大三个区间？至少 1 个 ≤128 + 1 个 ≥4096
- [ ] `MODES` 是否包括对照基线？（改 FM 必须同时跑 HT 同 cell）
- [ ] 跑完结果第一行有没有写"本次实际覆盖 ... ，未覆盖 ..."？
- [ ] 这是修复类 / 算法改动 / metric 改动吗？是的话**不准缩范围**。
